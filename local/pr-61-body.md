Task 28 A/B'd the compatible on-policy `cfontes/GLM-5.3-Flash-DFlash2-TR3-v3` (`45f7e139`) against the standing `incoai/GLM-5.3-Flash-DFlash2` pin (`7d74cdd`) on the current production stack (`e3-w3-zfill`, TRF=32, adaptive-k ema, k=7, C4, KV pin). Independent variable was `DFLASH_MODEL` / `DFLASH_REVISION` only. Config oid `083085aa` is byte-identical; LFS oids `cfe8c069` vs `8931dc52`. Both-node Hub identity was verified before B. The drafter swap is a JIT-shape change; caches were wiped both directions through the guarded unit.

Cluster A vs B: hashmap 30.37 → 29.71 tok/s (−2.2%), hard essay 24.27 → 23.42 (−3.5%). Structured stayed 7.0/1.000 (68.44 → 69.12). Cold prefill wash on the kit's standing unique-prompt ladder (`prefill-probe.py --tokens 60000/240000`; actual usage ~80k/~320k, same oversize as W2/E3): 60k 1447.5 → 1455.0, 240k 1368.3 → 1369.9. B passed acceptance 7/7, serving 6/6, and toolcall `--quick` 16/16 (not the full 23/23 battery). Pool stayed 1,396,551 / 1.40×.

Verdict: REVERT. The ≥5% prose/agentic adopt bar was not met. The publisher TP4/K=2 table does not transfer to this TP2 / k=7 / adaptive-k stack. B2 and a full return-A decode/prefill rebench were skipped because B already missed the bar. Production last-wins restored `7d74cdd`. Return-A restoration smoke: structured 70.15 @ 7.0/1.000, acceptance 7/7, serving 6/6, watchdog re-armed. Candidate bytes may stay on disk; do not redistribute (CC BY-NC-ND base). Do not chain k=2.

This PR records the window: env.example pin comment, docs/02 / docs/06 / docs/10 / docs/13, dflash pin-guard test, and cluster receipts under `local/task28/`.
